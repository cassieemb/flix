<section class = "container">
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= link_to(user.name, user_path(user)) %>:
        account created <%= time_ago_in_words(user.created_at)%> ago</li>
    <% end %>
  </ul>
</section>